diff --git a/main.c b/main.c
--- a/main.c
+++ b/main.c
@@ -59,2 +59,3 @@ static void usage(const char *prog) {
     fprintf(stderr, "  --stream      Streaming mode: process in chunks with prefix rollback\n");
+    fprintf(stderr, "  --stream-unfixed-chunks <n>  Chunks withheld before the first stable commit (default: 2)\n");
     fprintf(stderr, "  --stream-max-new-tokens <n>  Max generated tokens per stream step (default: 32)\n");
@@ -83,3 +84,4 @@ int main(int argc, char **argv) {
     int stream_mode = 0;
     int stream_max_new_tokens = -1; /* -1 = use default (32) */
+    int stream_unfixed_chunks = -1; /* -1 = use default (2) */
     float enc_window_sec = -1;   /* -1 = use default (8s) */
@@ -103,4 +105,6 @@ int main(int argc, char **argv) {
             stream_mode = 1;
         } else if (strcmp(argv[i], "--stream-max-new-tokens") == 0 && i + 1 < argc) {
             stream_max_new_tokens = atoi(argv[++i]);
+        } else if (strcmp(argv[i], "--stream-unfixed-chunks") == 0 && i + 1 < argc) {
+            stream_unfixed_chunks = atoi(argv[++i]);
         } else if (strcmp(argv[i], "--enc-window-sec") == 0 && i + 1 < argc) {
@@ -150,5 +154,9 @@ int main(int argc, char **argv) {
     if (stream_max_new_tokens == 0 || stream_max_new_tokens < -1) {
         fprintf(stderr, "Error: --stream-max-new-tokens must be > 0\n");
         return 1;
     }
+    if (stream_unfixed_chunks < -1 || stream_unfixed_chunks > 8) {
+        fprintf(stderr, "Error: --stream-unfixed-chunks must be in [0, 8]\n");
+        return 1;
+    }
     if (input_wav && use_stdin) {
@@ -180,4 +188,5 @@ int main(int argc, char **argv) {
         ctx->config.enc_n_window_infer = window_frames;
     }
     if (stream_max_new_tokens > 0) ctx->stream_max_new_tokens = stream_max_new_tokens;
+    if (stream_unfixed_chunks >= 0) ctx->stream_unfixed_chunks = stream_unfixed_chunks;
     if (past_text_conditioning_mode >= 0)
